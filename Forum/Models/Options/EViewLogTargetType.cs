namespace Forum.Models.Options {
	public enum EViewLogTargetType {
		All,				// mark all topics read for the entire site
		Message,			// mark single message as read
		Topic				// mark single topic as read
	}
}